# Performance baseline — 2026-05-28

Captured against production after Phase 0 (Server-Timing instrumentation)
deploys but before Phase 1 (signed-URL image delivery) lands.  Phase 1
will re-capture these numbers and we'll diff to confirm the win.

> **Action required from human operator after Phase 0 merges:**
>
> 1. Open `https://<your-vercel-prod-domain>/patients` on your phone (or
>    Chrome DevTools mobile emulation).  Sign in.  Open DevTools → Network.
>    Filter for `api/files`.  Click 5 thumbnail rows.  Record the
>    `server-timing` response header for each — paste into "Raw samples"
>    below.
>
> 2. Run two Lighthouse mobile runs:
>    ```
>    pnpm exec lighthouse https://<prod>/patients --preset=mobile \
>      --quiet --output=json --output-path=./tmp/lh-patients.json
>    pnpm exec lighthouse https://<prod>/patients/<some-id> --preset=mobile \
>      --quiet --output=json --output-path=./tmp/lh-patient.json
>    ```
>    Record FCP / LCP / TBT / Speed Index / LCP element for each below.
>
> 3. Snapshot the Vercel dashboard "Last 30 days" tile and copy each
>    metric below.
>
> 4. Commit with `docs(perf): fill Phase 0 baseline numbers`.

## /api/files response timings (median of 5)

| Stage   | Median (ms) |
|---------|-------------|
| auth    | <fill>      |
| db      | <fill>      |
| storage | <fill>      |
| total   | <fill>      |

### Raw samples

```
<paste 5 server-timing headers here>
```

## Lighthouse — mobile preset

### `/patients`
- FCP:           <fill> ms
- LCP:           <fill> ms
- TBT:           <fill> ms
- Speed Index:   <fill> ms
- LCP element:   <fill>

### `/patients/<id>` (use a long-stay patient if available)
- FCP:           <fill> ms
- LCP:           <fill> ms
- TBT:           <fill> ms
- Speed Index:   <fill> ms
- LCP element:   <fill>

## Vercel dashboard snapshot (Last 30 days)

| Metric                            | Used / Cap   |
|-----------------------------------|--------------|
| Fast Origin Transfer              | 6.81 / 10 GB |
| Fluid Active CPU                  | 1h 24m / 4h  |
| Function Invocations              | 63K / 1M     |
| Edge Requests                     | 48K / 1M     |
| Fast Data Transfer                | 4.25 / 100 GB|
| Fluid Provisioned Memory          | 9.1 / 360 GB-Hrs |
| Image Optimization Cache Writes   | 1.2K / 100K  |
| Image Optimization Transformations| 45 / 5K      |
| ISR Reads                         | 1.7K / 1M    |
| Edge Request CPU Duration         | 6s / 1h      |

> Source: dashboard screenshot 2026-05-28 (cycle started ~2026-05-01,
> resets ~2026-06-01).
